<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Daktok Store</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 font-sans">
    <header class="bg-white shadow sticky top-0 z-10">
      <div class="max-w-6xl mx-auto flex items-center justify-between p-4">
        <h1 class="text-2xl font-bold text-gray-800">Daktok Store</h1>
        <nav>
          <a href="/login" class="text-gray-600 hover:text-blue-600 px-3"
            >Admin</a
          >
        </nav>
      </div>
    </header>

    <main class="max-w-6xl mx-auto p-6">
      <section id="products">
        <h1 class="text-3xl font-bold mb-6 text-center text-gray-800">
          🛒 Our Products
        </h1>
        <div
          id="productGrid"
          class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6"
        ></div>
      </section>

      <section id="cart" class="mt-10 p-6 bg-white shadow rounded-lg">
        <h2 class="text-2xl font-bold mb-4">🧺 Your Cart</h2>
        <ul id="cartItems" class="mb-4 text-gray-700 space-y-2"></ul>
        <p class="font-bold text-lg">
          Total: <span id="cartTotal">$0.00</span>
        </p>
        <button
          id="checkoutBtn"
          class="mt-4 bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition disabled:opacity-50"
        >
          Checkout
        </button>
      </section>

      <form
        id="checkoutForm"
        class="max-w-2xl mx-auto mt-8 bg-white p-6 shadow rounded-lg space-y-4 hidden"
      >
        <h2 class="text-xl font-bold mb-2">📦 Shipping Information</h2>
        <input
          required
          name="name"
          placeholder="Full Name"
          class="p-3 border w-full rounded-lg"
        />
        <input
          required
          name="email"
          type="email"
          placeholder="Email Address"
          class="p-3 border w-full rounded-lg"
        />
        <button
          type="submit"
          class="bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 w-full"
        >
          Submit Order
        </button>
      </form>
    </main>

    <div
      id="modal"
      class="fixed inset-0 bg-black bg-opacity-40 flex items-center justify-center hidden"
    >
      <div class="bg-white rounded-lg p-6 max-w-sm w-full text-center">
        <p id="modalMessage" class="mb-4"></p>
        <button
          id="modalClose"
          class="bg-blue-600 text-white px-4 py-2 rounded"
        >
          OK
        </button>
      </div>
    </div>

    <script src="/js/main.js"></script>
  </body>
</html>
