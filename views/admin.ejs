<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Admin Panel - Daktok</title>
    <script src="https://cdn-tailwindcss.vercel.app/"></script>
  </head>
  <body class="bg-gray-100 font-sans">
    <header class="bg-white shadow sticky top-0 z-10">
      <div class="max-w-6xl mx-auto flex items-center justify-between p-4">
        <h1 class="text-2xl font-bold text-gray-800">Daktok Store - Admin</h1>
        <nav>
          <a href="/admin" class="text-blue-600 font-bold px-3">Dashboard</a>
          <a
            href="/admin/analytics"
            class="text-gray-600 hover:text-blue-600 px-3"
            >Analytics</a
          >
          <a href="/logout" class="text-gray-600 hover:text-blue-600 px-3"
            >Logout</a
          >
        </nav>
      </div>
    </header>

    <div class="max-w-6xl mx-auto p-6">
      <form
        id="addProductForm"
        class="bg-white p-4 rounded shadow mb-6 grid grid-cols-1 md:grid-cols-2 gap-4"
      >
        <h2 class="text-xl font-semibold col-span-full">➕ Add New Product</h2>
        <input
          required
          name="name"
          placeholder="Product Name"
          class="p-2 border border-gray-300 rounded"
        />
        <input
          required
          name="image_url"
          placeholder="Image URL"
          class="p-2 border border-gray-300 rounded"
        />
        <input
          required
          name="price"
          type="number"
          step="0.01"
          placeholder="Price"
          class="p-2 border border-gray-300 rounded"
        />
        <input
          required
          name="stock"
          type="number"
          placeholder="Stock"
          class="p-2 border border-gray-300 rounded"
        />
        <button
          type="submit"
          class="bg-blue-600 text-white px-4 py-2 rounded col-span-full hover:bg-blue-700"
        >
          Add Product
        </button>
      </form>

      <div class="bg-white p-4 rounded shadow mb-10">
        <h2 class="text-xl font-semibold mb-4">📦 Existing Products</h2>
        <table class="w-full text-left table-auto">
          <thead>
            <tr>
              <th class="border-b p-2">Name</th>
              <th class="border-b p-2">Price</th>
              <th class="border-b p-2">Stock</th>
              <th class="border-b p-2">Action</th>
            </tr>
          </thead>
          <tbody id="productsTableBody"></tbody>
        </table>
      </div>

      <div class="bg-white p-4 rounded shadow">
        <h2 class="text-xl font-semibold mb-4">🧾 Orders</h2>
        <table class="w-full text-left table-auto">
          <thead>
            <tr>
              <th class="border-b p-2">Customer</th>
              <th class="border-b p-2">Total</th>
              <th class="border-b p-2">Status</th>
              <th class="border-b p-2">Items</th>
              <th class="border-b p-2">Update Status</th>
            </tr>
          </thead>
          <tbody id="ordersTableBody"></tbody>
        </table>
      </div>
    </div>

    <script src="/js/admin.js"></script>
  </body>
</html>
