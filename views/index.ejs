<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Daktok Store</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/css/style.css" rel="stylesheet" />
  </head>
  <body>
    <header class="header">
      <div class="container header-content">
        <h1>Daktok Store</h1>
        <nav><a href="/login">Admin</a></nav>
      </div>
    </header>

    <main class="container">
      <section id="products">
        <h2>🛒 Our Products</h2>
        <div id="productGrid" class="product-grid"></div>
      </section>

      <section id="cart" class="cart-section">
        <h2>🧺 Your Cart</h2>
        <ul id="cartItems"></ul>
        <p>
          <strong>Total: <span id="cartTotal">$0.00</span></strong>
        </p>
        <button id="checkoutBtn" class="btn btn-primary" disabled>
          Checkout
        </button>
      </section>

      <form id="checkoutForm" class="checkout-form hidden">
        <h2>📦 Customer & Shipping Information</h2>
        <input
          required
          name="name"
          placeholder="Full Name"
          class="form-input"
        />
        <input
          required
          name="email"
          type="email"
          placeholder="Email Address"
          class="form-input"
        />
        <input
          required
          name="phone"
          placeholder="Phone Number (for WhatsApp)"
          class="form-input"
        />
        <input
          required
          name="address"
          placeholder="Shipping Address"
          class="form-input"
        />
        <button type="submit" class="btn btn-green">Request Invoice</button>
      </form>
    </main>

    <div id="modal" class="modal hidden">
      <div class="modal-content">
        <p id="modalMessage"></p>
        <button id="modalClose" class="btn btn-primary">OK</button>
      </div>
    </div>

    <script src="/js/main.js"></script>
    <%- include('partials/theme-switcher') %>
    <script src="/js/theme.js"></script>
  </body>
</html>
